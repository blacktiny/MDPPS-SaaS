import React from 'react'
import { RouteComponentProps } from '@reach/router'

const OnboardingUserRoot: React.FC<RouteComponentProps> = ({ children }) => {
  return <>{children}</>
}

export default OnboardingUserRoot
