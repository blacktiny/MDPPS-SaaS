import React from 'react'
import { RouteComponentProps } from '@reach/router'

const Auth: React.FC<RouteComponentProps> = ({ children }) => <React.Fragment>{children}</React.Fragment>

export default Auth
